# Accessibility Review Extension

Adds accessibility expectations for products with UI surfaces.

Checks should cover keyboard access, labels, contrast, focus states, responsive behavior, and screen-reader semantics.
