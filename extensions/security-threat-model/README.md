# Security Threat Model Extension

Adds a mandatory security review before readiness when a product touches auth, authorization, sensitive data, payment, public APIs, or external providers.

Expected artifact:

```text
docs/security/threat-model.md
```
